<!-- kanibako-managed loader — do NOT edit; kanibako owns this file. Delivered read-only to ~/.config/kanibako/kickoff.md as the SEED the box-start flattener resolves into goose's native slot (~/.config/goose/AGENTS.md, loaded every session). Its sole job is to pull in the canon entry point: ~/canon/COLLECTION.md (the kanibako-owned index of the bible / handbook / notebook books) and, through it, user-configured directives. ⚑ TRANSITION (one release only): the second line is the PRE-CANON path, kept so this plugin release also works against a base that still binds the old rom layout — the flattener degrades a missing import target to an inert backticked mention plus an 'unresolved import' warning on stderr, so whichever base is installed exactly one line resolves and the other is visibly, harmlessly inert. Expect one such warning per launch until the transition closes. ⚑ PUBLISH ORDER: PLUGINS FIRST, THEN THE BASE — a NEW base with an OLD plugin is the one fatal combination (that kickoff carries only the legacy line, which the new base deleted, so NOTHING resolves). REMOVE the legacy line once the canon base is the published floor (migration M-12). -->
@~/canon/COLLECTION.md
@~/playbook/kanibako/directives/KANIBAKO.md
