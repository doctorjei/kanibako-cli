<!-- kanibako-managed loader — do NOT edit; kanibako owns this file. Delivered read-only to ~/.config/kanibako/kickoff.md as the SEED the box-start flattener resolves into claude's native user-memory slot (~/.claude/CLAUDE.md, loaded every session). Its sole job is to pull in the canon entry point: ~/canon/COLLECTION.md (the kanibako-owned index of the bible / handbook / notebook books) and, through it, user-configured directives. ⚑ ONE IMPORT, AND IT IS THE CANON ONE (migration M-12): the pre-canon transition line this kickoff used to carry is GONE, because the tree it addressed is gone — that content moved into the canon, which ~/canon/COLLECTION.md already reaches. ⚑ REQUIRED BASE: one that binds the canon — kanibako-cli 1.8.0 or newer. Against an older base NOTHING here resolves and every directive silently stops loading, so base and plugins must move together: upgrade through the `kanibako` meta package, or upgrade both sides in one step. -->
@~/canon/COLLECTION.md
