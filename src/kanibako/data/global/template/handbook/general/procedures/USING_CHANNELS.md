# Procedure — Channels: Etiquette & Inbox Discipline

> **PROCEDURE, not auto-loaded.** How to send, where to write, & how to handle what arrives.
> **Read this the moment ANY inter-instance message is involved** — at the session-start mailbox
> check, when a message arrives mid-session, or before writing to a mailbox/share/chat. ⚑ You are
> the PRIMARY HANDLER of messages, not a relay; skipping this rule is how a message gets silently
> relayed or dropped, & how another box's history gets overwritten.
> Always-on conduct (be kind & honest, be concise) stays in
> `~/canon/handbook/general/directives/rules/INTERACTION.md` (auto-loaded).
> Moved out of `directives/` 2026-08-04 (context cost).

## Etiquette

- **Be professional** in tone & approach.
- **Address mailboxes properly** — write into recipient's `mailboxes/<workset>/<box>/` (not yours).
- **Append to chat logs; don't rewrite** — boxes share the file. Add your line & leave other content as-is.
- **Respect share & mailbox conventions** (who reads, who writes); they aren't enforced.
- **Sign messages as `$KANIBAKO_NAME`.**

## Inbox discipline — you own your conversations

You are the **primary handler** of inter-instance messages, not a relay.

1. **Triage** each message — does it need a response? Act on it.
2. **Respond directly** before pulling in the human operator.
3. **Tell the operator briefly** what came in & what you did (a one-line summary).
4. **Track replied status** — rename a handled file with a `.replied` suffix (`foo.md` →
   `foo.replied.md`) so unhandled messages stay obvious.
